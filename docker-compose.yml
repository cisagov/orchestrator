version: '3.2'

secrets:
  cyhy_read_creds:
    file: ./secrets/cyhy_read_creds.yml
  scan_read_creds:
    file: ./secrets/scan_read_creds.yml
  scan_write_creds:
    file: ./secrets/scan_write_creds.yml
  aws_config:
    file: ./secrets/aws_config

services:
  redis:
    image: 'redis:alpine'
    # ports:
    #   - target: 6379
    #     published: 6379
    #     protocol: tcp
    #     mode: host
  gather:
    image: 'dhsncats/gatherer:1.2.1'
    depends_on:
      - redis
    secrets:
      - source: cyhy_read_creds
        target: database_creds.yml
    volumes:
      - ./output:/home/gatherer/shared
  scan:
    image: 'dhsncats/scanner:1.1.8'
    depends_on:
      - redis
    secrets:
      - source: aws_config
        target: aws_config
    environment:
      - AWS_CONFIG_FILE=/run/secrets/aws_config
      - AWS_PROFILE=lambda
    volumes:
      - ./output:/home/scanner/shared
  save:
    image: 'dhsncats/saver:1.1.0'
    depends_on:
      - redis
    secrets:
      - source: scan_write_creds
        target: scan_write_creds.yml
    volumes:
      - ./output:/home/saver/shared
  trustymail_report:
    image: 'dhsncats/trustymail_reporter:1.1.9'
    depends_on:
      - redis
    secrets:
      - source: scan_read_creds
        target: scan_read_creds.yml
      - source: aws_config
        target: aws_config
    environment:
      - AWS_CONFIG_FILE=/run/secrets/aws_config
      - AWS_PROFILE=elasticsearch
    volumes:
      - ./output:/home/reporter/shared
  pshtt_report:
    image: 'dhsncats/pshtt_reporter:1.1.3'
    depends_on:
      - redis
    secrets:
      - source: scan_read_creds
        target: scan_read_creds.yml
    volumes:
      - ./output:/home/reporter/shared
